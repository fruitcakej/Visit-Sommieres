<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/primary_dark_color</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CustomActionBar" >
        <item name="android:windowActionBarOverlay">true</item>
        <!-- Support library compatibility -->
        <item name="windowActionBarOverlay">true</item>
    </style>

    <!-- styling for custom tab -->
    <style name="CustomTabLayout">
        <item name="tabTextColor">#214061</item>
        <item name="tabBackground">@color/color_primary</item>
        <item name="tabIndicatorColor">#FFFFFF</item>
        <item name="tabSelectedTextColor">#FFFFFF</item>
    </style>

    <!-- Style for home page text-->
    <style name="home">
        <item name="android:padding">@dimen/padding_16dp</item>
        <item name="android:layout_margin">@dimen/padding_16dp</item>
        <item name="android:background">@drawable/bg_gradient</item>
        <item name="android:lineSpacingMultiplier">1.3</item>
        <item name="android:lineSpacingExtra">3dp</item>
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Style for Video Buttons-->
    <style name="video_buttons">
        <item name="android:layout_marginLeft">@dimen/padding_16dp</item>
        <item name="android:layout_marginRight">@dimen/padding_16dp</item>
        <item name="android:layout_marginBottom">@dimen/padding_8dp</item>
    </style>

    <!-- Style for Pop Over overlay-->
    <style name="AppTheme.popup">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>